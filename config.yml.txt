backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "pages"
    label: "Pages"
    files:
      - file: "menu.json"
        label: "Menu"
        name: "menu"
        fields:
          - { label: "Menu Items", name: "items", widget: "list" }